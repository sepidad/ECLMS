"""Reporting domain package.

The reporting module computes derived analytics and introduces no new
domain aggregates of its own (RPT-022 section 2: reports are derived,
never stored as source truth).
"""

__all__: list[str] = []